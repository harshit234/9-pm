# 9-pm
Tuple creation, single-element tuple gotcha, tuple unpacking, named tuples, tuple as dictionary key, set creation, set operations (union, intersection, difference, symmetric_difference), set methods (add, remove, discard), frozenset, performance (O(1) lookup)
